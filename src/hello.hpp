#include <string>
// compare 2 strings
// return 0 if equal
// return negative if first_string < second_string
// return positive if first_string > second_string

int strcmp_case_insensitive(const char* first_string, const char* second_string);
